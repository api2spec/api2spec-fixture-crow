services:
  app:
    build: .
    ports:
      - "8080:8080"

  dev:
    image: gcc:13
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    command: bash
    stdin_open: true
    tty: true
